🌀 Panorama Viewer – Run Guide

🇺🇸 How to Run the Project (English)

There are two simple ways to run this project:

📘 Method 1 – Using Node.js

Make sure Node.js is installed on your system.

Open a terminal in this folder and run:

npm install -g http-server
npx http-server -p 3000


Open one of the following URLs (or the address shown in the terminal):

http://127.0.0.1:3000
http://<your-local-ip>:3000

🐍 Method 2 – Using Python

If Python is installed, run the following command in this folder:

python run-python.py


The browser will open automatically:

http://localhost:8080/panorama.html

📂 Note

The panoramic.jpg file must be located in the same directory as the project files.
------------------------------------
🇮🇷 راهنمای اجرای پروژه (فارسی)

برای اجرای این پروژه، دو روش ساده وجود دارد:

📘 روش ۱ – با Node.js

مطمئن شوید Node.js روی سیستم نصب است.

در همین پوشه ترمینال باز کنید و دستورات زیر را اجرا کنید:

npm install -g http-server
npx http-server -p 3000


یکی از آدرس‌های زیر (یا آدرس‌های نمایش‌داده‌شده در ترمینال) را در مرورگر باز کنید:

http://127.0.0.1:3000
http://<your-local-ip>:3000

🐍 روش ۲ – با Python

اگر Python روی سیستم نصب است، در همین پوشه دستور زیر را اجرا کنید:

python run-python.py


مرورگر به‌صورت خودکار باز می‌شود:

http://localhost:8080/panorama.html

📂 نکته

فایل panoramic.jpg باید در همین پوشه پروژه قرار داشته باشد.
